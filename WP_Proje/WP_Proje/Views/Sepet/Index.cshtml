@model IEnumerable<WP_Proje.Models.Sepet>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SepetFiyat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kullanici)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SepetFiyat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kullanici.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SepetId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SepetId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SepetId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
